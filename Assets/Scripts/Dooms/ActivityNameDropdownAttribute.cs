using UnityEngine;

namespace MLA_SIM.Dooms
{
    /// <summary>
    /// Inspector dropdown backed by ActivityCatalogSO activity names.
    /// Falls back to text input when no catalog instance is available.
    /// </summary>
    public class ActivityNameDropdownAttribute : PropertyAttribute
    {
    }
}
